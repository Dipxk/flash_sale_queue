#!/bin/bash -e

# Prepare DB when booting the web process (Fly process group "web" or rails server)
if [[ "${1}" == "./bin/rails" && "${2}" == "server" ]] || [[ "${FLY_PROCESS_GROUP}" == "web" ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
